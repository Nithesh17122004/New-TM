# Coturn turnuserdb.conf — Thooku Madurai
# Credentials the backend will send to clients. Generate the key with:
#   turnadmin -k -u thooku -r thookumadurai
# Then put the resulting long hex key below (replace this example value).
# The backend env vars must match:
#   TURN_SERVER_URL=turn:YOUR_VPS_IP:3478
#   TURN_SERVER_USERNAME=thooku
#   TURN_SERVER_CREDENTIAL=<the plaintext password that hashes to the key below>
thooku:0xREPLACE_WITH_TURNADMIN_KEY:thookumadurai